type showcaseItems = [
    items: [showcaseItem]
]

type showcaseItem = {
    title: string,
    image: string,
    link: string,
    shortDescription: string,
    description: [string, string, string]
}